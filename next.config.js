/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['avatars.githubusercontent.com', 'avatar.vercel.sh', 'ucarecdn.com', 'lh3.googleusercontent.com']
  }
};

module.exports = nextConfig;
